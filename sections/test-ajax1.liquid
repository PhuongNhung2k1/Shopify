<script src="{{ 'test-ajax1.js' | asset_url }}" defer="defer"></script>
<!DOCTYPE html>
<html>
  <body>

    <div id="demo">
      <h2>The XMLHttpRequest Object</h2>
      <button type="button" onclick="loadDoc()">Change Content</button>
    </div>

    <script>
      function loadDoc() {
        const xhttp = new XMLHttpRequest();
        xhttp.onload = function() {
          document.getElementById("demo").innerHTML = this.responseText;
        }
        xhttp.open("GET", "test-minicart.liquid");
        xhttp.send();
      }
    </script>

  </body>
</html>

{% schema %}
  {
    "name": "Tesst ajax",
    "blocks": [
      {
        "type": "type_ajax",
        "name": "Ajax...",
        "settings": [
          {
            "type": "text",
            "id": "test_ajax",
            "label": "Test show ajax"
          }
        ]
      }
    ],

    "presets": [
      {
        "name": "Test Show ajax",
        "blocks": [
          {
            "type": "type_ajax"
          }
        ]
      }
    ]
  }
{% endschema %}